PORT=3001
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=sentisphere_ai
JWT_SECRET=change_this_secret_for_production

# Required for employee feedback: Python FastAPI stress model (model/app.py).
STRESS_API_URL=http://127.0.0.1:8000
# Text-only feedback: 15000 is enough. Voice upload + Whisper + stress: use 120000 or higher.
# STRESS_API_TIMEOUT_MS=120000
